{
	"version": "0.2.0",
	"configurations": [{
		   "name": "Flutter",
		   "request": "launch",
		   "type": "dart",
		   "args": [
			   "-d", "web-server", // Start a web server
				"--web-port", "8080", // bind to port 8080
				"--web-hostname", "0.0.0.0", // bind to  0.0.0.0 instead of 127.0.0.1, so we can access it from outside
			],
	   }
   ]
}
   